export enum BlockType {
  Section = 'section',
  Markdown = 'markdown',
  Actions = 'actions',
  Context = 'context',
  Input = 'input',
  File = 'file',
  Divider = 'divider',
  Image = 'image',
  Header = 'header',
  Video = 'video',
}
